# บทพูดประกอบสไลด์ · AI Product Workshop (ภาษาไทย)

เอกสารนี้เรียงตาม `slides.html` จำนวน 21 หน้า สไลด์ 03–20 ตรงกับบท 00–17 ใน `content.js` ใช้เป็นบทพูดสำหรับผู้สอน ไม่ใช่ข้อความที่ต้องอ่านจากสไลด์ทุกคำ เปิด “เนื้อหาบทนี้” บนสไลด์เมื่อจะสาธิตรายละเอียดหรือให้ผู้เรียนลงมือทำ เวลาแต่ละบทเป็นเวลาสำหรับเนื้อหาและแบบฝึก ไม่ใช่เวลาพูดบนสไลด์หน้าเดียว

## สไลด์ 01 · เปิดคอร์ส

“ยินดีต้อนรับสู่ AI Product Workshop คอร์สนี้ออกแบบให้ PM, BA และ Product Design คุมงานร่วมกับ AI Agent ได้ตั้งแต่ requirement จนถึง prototype ที่กดใช้งานและตรวจสอบได้ เราจะใช้เรื่องเดียวต่อเนื่องกันคือ **US-001 OPD Patient Check-in Lite** เพื่อให้เห็นว่างานแต่ละชิ้นส่งต่อกันอย่างไร

เป้าหมายของเราไม่ใช่ให้ทุกคนเขียนโค้ดเป็น แต่ให้ทุกคนบอกได้ว่าอะไรควรเกิดขึ้น ตรวจหลักฐานได้ว่าสิ่งนั้นเกิดขึ้นจริง และส่งงานให้ Developer ทำต่อได้ ตัวอย่างผู้ป่วยทั้งหมดเป็นข้อมูลสมมติ เราจะไม่เชื่อมระบบโรงพยาบาลจริง ระหว่างเรียน ถ้าติดตรงไหนให้จดคำถามไว้พร้อมสิ่งที่เห็นบนหน้าจอ เพราะหลักฐานนั้นจะช่วยเราในบท debugging ด้วย”

## สไลด์ 02 · แผนที่การเรียน: 3 วัน + Capstone

“มองแผนนี้จากซ้ายไปขวา วันที่หนึ่งเราจะเปลี่ยน requirement ให้เป็น Issue จริง แล้วแยก branch กับ worktree ก่อนให้ Agent สำรวจและเสนอแผน วันที่สองเราจะอ่านโครงสร้าง Next.js วางกติกาการออกแบบ แตกหน้าเป็น component และตรวจ state ผ่าน Storybook วันที่สามเราจะใช้ mock data ต่อ flow ทั้งหน้า เก็บหลักฐานเวลาเกิดปัญหา ตรวจ diff และส่ง Draft MR พร้อม handoff

ทุกวันทำต่อบน US-001 เรื่องเดิม เมื่อผู้ตรวจ merge งานแรกแล้ว Capstone จะเปิด Issue ใหม่เรื่องคลินิกไม่พร้อมรับ check-in และพิสูจน์ว่าพฤติกรรมเดิมของ US-001 ยังทำงานอยู่ ระหว่างทางให้จำสายการเชื่อมโยงนี้ไว้: **เลข Issue → ชื่อ branch → footer ของ commit → `Closes` ใน MR** เราจะกลับมาตรวจสายนี้ตอนส่งงาน”

## สไลด์ 03 · บท 00: เตรียมพร้อม

“ก่อนเริ่มงานจริง เราต้องมีพื้นที่ที่เปิดใช้งานได้ ผู้เรียนแต่ละคนต้องมี repo ของตัวเองบน GitHub หรือ GitLab เพื่อเปิด Issue, push branch และเปิด MR ได้ ตรวจ Git identity และเครื่องมือที่จำเป็น แล้วเปิดทั้ง Next.js กับ Storybook จาก starter ให้สำเร็จ

ภาพนี้แสดงบทบาทของเครื่องมือ: Agent ช่วยสำรวจและแก้ไฟล์, Git บันทึกสิ่งที่เปลี่ยน, Node.js รันเว็บ, ส่วนเราเป็นคนดูผลใน browser และตัดสินใจ หากคำสั่งใดไม่ผ่าน ให้เก็บข้อความผิดพลาดไว้ตามจริง ไม่ต้องเดาแก้ไปเรื่อย ๆ กฎของคอร์สคือใช้เฉพาะ mock หรือ synthetic data ห้ามนำข้อมูลผู้ป่วยจริง secret หรือ endpoint จริงมาใส่ใน prompt เมื่อจบบทนี้ เราควรมี repo ของตัวเองและ starter ที่รันได้”

## สไลด์ 04 · บท 01: โมเดลความคิด

“ตอนนี้ขอดูภาพรวมของงานหนึ่งชิ้น เริ่มจาก requirement ที่บอกความต้องการ แล้วแปลงเป็น Issue ที่มีเกณฑ์ตรวจได้ จากนั้นจึงทำใน branch และ worktree ให้ Agent ช่วยสร้าง component กับ Storybook story ประกอบเป็นหน้าเว็บ ตรวจด้วยคนและเครื่องมือ แล้วส่งผ่าน MR

จุดที่อยากให้จำคือ Agent ทำงานเร็ว แต่ไม่ได้ตัดสินแทนเราว่า requirement หมายถึงอะไร คนต้องกำหนดผลลัพธ์ ตรวจ scope และอนุมัติแผนก่อนแก้ไฟล์ คำว่า ‘Agent บอกว่าเสร็จ’ ยังไม่เท่ากับงานผ่าน เพราะเรายังต้องเห็น UI จริง อ่าน diff และตรวจ acceptance criteria ลองชี้ในภาพว่าขั้นไหนเป็นการตัดสินใจของคน และขั้นไหนเป็นงานที่มอบให้ Agent ทำได้”

## สไลด์ 05 · บท 02: Requirement → Issue

“US-001 เป็น requirement ต้นทาง แต่ Agent ต้องการ Issue ที่เริ่มงานได้ทันที ให้เปิดเอกสาร requirement ใน starter อ่าน acceptance criteria ให้ครบ แล้วเขียน Issue ที่บอก context, ขอบเขต, สิ่งที่ไม่ทำ และ Definition of Done อย่างชัดเจน งานนี้ต้องเล็กพอสำหรับ branch เดียว

เราแยก AC เป็นสองชนิด: **state** เช่นหน้าจอ Loading หรือ Empty ซึ่งเปิดตรวจใน Storybook ได้ และ **behavior** เช่นกด Confirm โดยไม่เลือกคลินิกแล้วต้องเห็น validation ซึ่งตรวจด้วย interaction test หรือเดิน flow จริง ค่า mock ต้องคงที่เพื่อให้ทุกคนทำซ้ำได้ ก่อนกดสร้าง Issue ให้ถามว่าแต่ละข้อผ่านหรือไม่ผ่านได้จริงไหม เมื่อได้เลข Issue แล้วจดไว้ เพราะเลขเดียวกันต้องตามไปถึง branch, commit footer และ MR”

## สไลด์ 06 · บท 03: Git พื้นฐาน

“สำหรับคอร์สนี้ Git คือเครื่องมือดูว่า Agent เปลี่ยนอะไร ไม่จำเป็นต้องจำคำสั่งทุกตัว เริ่มจาก `git status` เพื่อรู้ว่าเราอยู่ branch ไหนและมีไฟล์เปลี่ยนหรือไม่ แล้วใช้ `git diff` ดูเนื้อหาที่เปลี่ยนก่อนบันทึก งานอยู่บน feature branch ของ Issue ห้ามแก้ `main` ตรง ๆ

Commit คือจุดบันทึกที่เราตรวจแล้ว ไม่ต้องรอให้ทั้ง Issue เสร็จ ข้อความใช้รูป `type(scope): subject` และ footer `Refs #<เลข Issue>` เพื่อย้อนรอยได้ ในคอร์สจะมี checkpoint หลัง component, interaction test, page flow และ bug fix หาก Agent เสนอคำสั่ง Git ที่เราอธิบายไม่ได้ ให้หยุดอ่านผลและถามก่อน โดยเฉพาะคำสั่งที่ลบประวัติหรือบังคับ push”

## สไลด์ 07 · บท 04: Git Worktree

“Branch คือสายงาน ส่วน worktree คือโฟลเดอร์ที่เปิดสายงานนั้นอยู่ เราจะใช้ **หนึ่ง Issue, หนึ่ง branch, หนึ่ง worktree** สำหรับ US-001 ตลอดคอร์ส วิธีนี้ทำให้ main อยู่ในพื้นที่ของมัน และ Agent ทำงานในพื้นที่ของ Issue โดยไม่เปลี่ยนไฟล์ใต้เท้ากัน

หลังสร้าง worktree ให้เปิด editor และ Agent จากโฟลเดอร์ใหม่นั้น ตรวจ `git worktree list` กับ `git status` ทุกครั้งที่ไม่แน่ใจว่าอยู่ที่ไหน แต่ละ worktree อาจต้องติดตั้ง dependency ของตัวเอง และถ้ารันเว็บหลายชุดพร้อมกันต้องใช้คนละ port ตัวเลขในตัวอย่าง branch เป็นเพียงตัวอย่าง ให้แทนด้วยเลข Issue ของตัวเอง เมื่อจบบทนี้ทุกคนควรตอบได้ว่า Agent กำลังแก้ไฟล์ในโฟลเดอร์ใด”

## สไลด์ 08 · บท 05: Explore → Plan → Human Review

“ตอนนี้เรามี Issue และพื้นที่ทำงานแล้ว อย่าเริ่มด้วยคำสั่งกว้าง ๆ ว่า ‘สร้างหน้า check-in’ เพราะ Agent จะเติมช่องว่างใน requirement ด้วยการเดา ให้เริ่มจากการอ่าน `AGENTS.md`, สำรวจ repo และเสนอแผนที่ระบุไฟล์, state, ขอบเขต และวิธีตรวจ โดยยังไม่แก้ไฟล์

หน้าที่ของเราคือเทียบแผนกับ AC ทีละข้อ: Loading, Empty, Error, validation และ success ครบหรือยัง มีไฟล์นอก scope หรือ dependency ใหม่โดยไม่จำเป็นไหม จะตรวจใน Storybook และหน้าเว็บอย่างไร ถ้าขาดให้ส่งกลับไปปรับแผน บทนี้หยุดเมื่อคนอนุมัติแผนและ `git status` ยังสะอาด เราจะเริ่มลงมือสร้างทีละส่วนในบทถัด ๆ ไป”

## สไลด์ 09 · บท 06: Next.js Literacy

“เราไม่ต้องอ่าน React ทุกบรรทัด แต่ควรอ่านแผนที่โปรเจกต์ออก ใน Next.js ตำแหน่ง `src/app/opd/check-in/page.tsx` บอก route `/opd/check-in` ส่วน feature เก็บ component และ logic ที่เกี่ยวข้อง และ mocks เก็บข้อมูลสมมติที่หน้าเว็บใช้

เวลา Agent เสนอแก้ไฟล์ ให้ถามสามข้อ: ไฟล์นี้อยู่ตรงไหน, เกี่ยวกับ Issue อย่างไร, และผู้ใช้จะเห็นผลอะไร ถ้าเห็น `<PatientCard />` ให้อ่านว่าเอา component มาใช้ ถ้าเห็น prop ให้อ่านว่าเป็นข้อมูลหรือ state ที่ส่งเข้าไป เปิด `package.json` เพื่อดูคำสั่ง dev, Storybook, lint, test และ build แล้วตรวจ path ที่ Agent อ้างว่ามีอยู่จริง”

## สไลด์ 10 · บท 07: Design System

“ก่อนสร้าง component ให้เริ่มจากภาพอ้างอิงการออกแบบใน starter ภาพบอกสิ่งที่มองเห็น เช่นลำดับสายตา สี และระยะห่าง แต่ไม่ได้บอกพฤติกรรมทุก state เราจะให้ Agent ร่าง `DESIGN.md` ที่มี token, หน้าที่ของ token, เหตุผล และสิ่งที่ยังไม่รู้ แล้วให้คนเทียบกับภาพจริงก่อนอนุมัติ

จากนั้นใช้ไฟล์เดียวกันทำหน้า `/design` ให้เปิดตรวจค่าได้ อย่าให้ Agent คิดสีหรือกติกาใหม่เองเพียงเพราะภาพไม่แสดงบางกรณี ให้บันทึกเป็น Known Gaps ไว้ บทนี้ยังไม่ commit เดี่ยว ๆ เราจะรวมไฟล์ design ที่ตรวจแล้วเข้ากับ component checkpoint แรกในบท Storybook”

## สไลด์ 11 · บท 08: Component & State

“หน้า check-in หนึ่งหน้าใหญ่เกินไปสำหรับการ review รอบเดียว ลองแตกเป็น PatientSearch, ผลการค้นหา, ฟอร์ม, หน้า preview และ success แต่ละส่วนควรมี state ที่ผู้ใช้เห็นจริง เช่น default, loading, empty, results และ error

เริ่มด้วยการเขียนรายการของตัวเองก่อน แล้วค่อยให้ Agent วิจารณ์ช่องว่างที่อาจพลาด ทุก state ควรโยงกลับไปหา AC ได้ ถ้าไม่มีเกณฑ์หรือผลต่อผู้ใช้ ก็ไม่ต้องสร้าง story เพิ่มเพื่อให้จำนวนดูเยอะ PM และ BA จะตรวจ business rule ส่วน Product Design จะตรวจข้อความ การมองเห็น focus และจอแคบ ผลลัพธ์ของบทนี้คือแผน component/state ที่คนอ่านและตรวจได้”

## สไลด์ 12 · บท 09: Storybook Review

“Storybook เป็นหน้าเว็บอีกชุดสำหรับเปิด component ทีละ state โดยไม่ต้องทำให้ระบบจริงเกิดเหตุการณ์นั้นก่อน เราจะเริ่มจาก PatientSearch ให้ Agent สร้าง component พร้อม stories แล้วเปิดดู Default, Loading, Empty, WithResults และ Error ด้วยตาของเรา

เวลาส่ง feedback ให้พูดเป็นภาษาผลิตภัณฑ์ เช่น ‘ข้อความ Empty ยังไม่บอกว่าควรทำอะไรต่อ’ หรือ ‘ชื่อยาวทำให้ปุ่มหายบนจอแคบ’ ไม่ต้องสั่งว่าจะเขียน CSS บรรทัดไหน ตรวจ keyboard และ focus ด้วย เมื่อ stories ผ่านการ review แล้วจึงบันทึก commit checkpoint แรกพร้อม `DESIGN.md` และหน้า `/design` ที่ตรวจแล้ว”

## สไลด์ 13 · บท 10: Acceptance & Interaction

“กลับมาที่ AC ของ Issue อีกครั้ง สำหรับ AC แบบ state ให้ระบุ Storybook story ที่เปิดดูได้ เช่น ‘ไม่พบผู้ป่วย’ ไปที่ Empty หรือ ‘บริการค้นหาล้มเหลว’ ไปที่ Error ส่วน AC แบบ behavior ให้เขียน Given/When/Then แล้วให้ Agent ทำ interaction test ที่กดและตรวจผลจริง

เราไม่ได้เพิ่ม test เพื่อให้มีตัวเลข แต่เพื่อให้คนเปิดหลักฐานแล้วเห็นว่าข้อกำหนดถูกตรวจตรงไหน ลองทำตาราง AC → story หรือ interaction แล้วหาข้อที่ยังไม่มีหลักฐาน เลือกหนึ่ง flow ที่สำคัญ รันทดสอบและดูมันทำงานทีละขั้น เมื่อผ่านแล้วจึงทำ commit checkpoint ที่สอง”

## สไลด์ 14 · บท 11: Mock Data

“ในระบบจริง UI จะขอข้อมูลจาก backend แต่คอร์สนี้เราสนใจว่าผู้ใช้เห็นอะไรระหว่างรอหรือเมื่อคำตอบผิดปกติ เราจึงใช้ mock service กับ synthetic data ให้สลับสถานการณ์ normal, slow, empty และ error ได้โดยไม่สร้าง backend หรือเชื่อมระบบจริง

Mock ที่ดีต้องทำซ้ำได้: input เดิมให้ผลเดิม เพื่อให้คนสองคนเปิดกรณีเดียวกันแล้วเห็นสิ่งเดียวกัน ลองสลับแต่ละ scenario แล้วดูว่า Loading อ่านทันไหม, Empty ช่วยผู้ใช้ต่ออย่างไร, Error บอกทางแก้หรือ retry ไหม หลังจากนั้นลองซ้ำรอบที่สอง ผลควรเหมือนเดิมทุกครั้ง”

## สไลด์ 15 · บท 12: Integration

“Component ที่ดูดีแยกกันอาจยังต่อเป็น flow ที่ดีไม่ได้ ตอนนี้ให้ Agent ประกอบหน้า `/opd/check-in` แล้วเราเดินทางผู้ใช้จริง: ค้นหา, เลือกผู้ป่วย, เลือกคลินิก, กรอก chief complaint หากต้องการ, preview, กลับไปแก้, confirm และดูผล success

ให้ลองกรณีที่มักพังเมื่อ state เชื่อมกัน เช่นค้นหาใหม่หลังเลือกคนแล้วข้อมูลเก่าค้างหรือไม่, ไม่เลือกคลินิกแล้ว validation ถูกจุดไหม, กลับจาก preview แล้วข้อมูลยังอยู่หรือเปล่า, กด Confirm ซ้ำเร็ว ๆ แล้วเกิดอะไรขึ้น ตรวจด้วย keyboard และจอกว้างประมาณ 375px แล้วเปิด Storybook ยืนยันว่า stories เดิมยังทำงาน เมื่อ flow ผ่านจึง commit checkpoint ที่สาม”

## สไลด์ 16 · บท 13: Debug ด้วยหลักฐาน

“เมื่อเจอปัญหา อย่าส่งแค่ ‘ช่วยแก้’ ให้ Agent เราจะเก็บหลักฐานจากสี่ที่: terminal บอก build หรือ type error, หน้า browser บอกสิ่งที่ผู้ใช้เห็น, Console บอก runtime error, และ Network บอกคำขอพร้อม response จากนั้นอ่านชนิด error, ข้อความ, ไฟล์ และบรรทัด เพื่อพอระบุตำแหน่งปัญหา

ให้ Agent อธิบาย root cause จากหลักฐานก่อนเสนอการแก้ แล้วเราทวนด้วยคำของตัวเอง หลังแก้ให้เล่นกรณีที่เคยพังซ้ำและดูว่า flow อื่นไม่เสีย กรณี port 3000 ถูกใช้งานเป็นปัญหาการรันหลาย process ไม่ใช่ bug ของ UI หลักฐานก่อนและหลังแก้ควรอยู่ใน commit checkpoint ที่สี่”

## สไลด์ 17 · บท 14: Git Diff & คุณภาพ

“ก่อนเชื่อว่างานเสร็จ ให้เดาก่อนว่า Issue นี้ควรเปลี่ยนไฟล์ประเภทไหน แล้วเทียบกับ `git status` และ `git diff --stat` ถ้า Issue เรื่อง PatientSearch แต่ diff ไปแตะ auth หรือ global layout ต้องถามเหตุผลให้ชัด เปิด diff ของไฟล์ที่ไม่คาดไว้และให้ Agent อธิบายทีละไฟล์

จากนั้นรัน lint, test และ build ตามคำสั่งใน project ตรวจ Storybook ทุก state และเดิน flow จริงอีกครั้ง ก่อน stage ให้ดูว่าไม่มี `.env`, secret, ข้อมูลผู้ป่วยจริง, build output หรือไฟล์ทดลองของ Agent ติดไปด้วย หลักฐานที่ต้องการคือ diff อยู่ใน scope และ quality checks ผ่าน ไม่ใช่เพียงข้อความสรุปว่า ‘done’”

## สไลด์ 18 · บท 15: ส่งมอบผ่าน Draft MR

“MR คือคำอธิบายงานให้คนที่ไม่ได้อยู่ในห้องนี้ตรวจต่อได้ เริ่มจากเก็บงานค้าง ตรวจ quality gates แล้ว sync branch กับ `main` เมื่อพร้อมจึง push feature branch และเปิด **Draft MR** ที่อธิบายเป้าหมาย, scope, states, ผลตรวจ, ขอบเขตของ mock และวิธีรัน

ให้ใส่ `Closes #<เลข Issue>` ที่ตรงกับ branch และ commit footer เสมอ Agent ช่วยร่าง MR จาก diff จริงได้ แต่คนต้องแก้ให้ตรงกับสิ่งที่ทดสอบจริง หาก reviewer ขอแก้ ให้แก้, ตรวจซ้ำ, commit และ push บน branch เดิม การ merge เข้าสู่ `main` เป็นการตัดสินใจของผู้ตรวจ หลัง merge จึงค่อยทำ cleanup ของ branch และ worktree”

## สไลด์ 19 · บท 16: ส่งต่อ Developer

“หน้าเว็บที่กดได้อย่างเดียวไม่พอสำหรับ handoff Developer ต้องรู้ว่า AC ข้อไหนมีหลักฐานจาก Storybook, interaction test หรือการเดินหน้าเว็บ ต้องรู้วิธีรัน app และ Storybook รวมทั้งรู้ว่า mock แทนระบบจริงตรงไหน และยังมีคำถามธุรกิจอะไรที่ต้องมีเจ้าของตัดสินใจ

ให้เรารวบรวมหลักฐานจาก Issue, MR, diff และผลตรวจที่มีอยู่แล้ว โดยระบุข้อจำกัดตามจริง เช่นยังไม่มี backend, auth หรือคิวจริง ลองให้คนอื่นใช้ขั้นตอน handoff เปิดงานโดยไม่ต้องถามเรา ถ้าเขาต้องเดา ให้เติมคำอธิบายหรือระบุ decision owner ในภายหลังสามารถทำ template หรือ skill ช่วยร่างซ้ำได้ แต่ก่อนส่งออกจากเครื่องคนยังต้องตรวจและอนุมัติ”

## สไลด์ 20 · บท 17: Capstone ต่อยอด US-001

“Capstone เริ่มหลังผู้ตรวจ merge US-001 แล้ว เราจะเปิด Issue ใหม่เรื่องคลินิกไม่พร้อมรับ check-in ไม่แก้ต่อบน branch เดิม งานใหม่ต้องมี AC ของตัวเอง ใช้ branch และ worktree ใหม่ แล้วเดินวงจร Explore, Plan, Human Review, Implement, Verify และ Draft MR อีกครั้ง

ให้แสดงกรณี GEN, ENT และ Error ด้วย mock ที่ทำซ้ำได้ ตรวจข้อความ การกู้คืนจาก error, keyboard และจอแคบ พร้อมหลักฐาน regression ว่า Search, Select, Preview และ Confirm เดิมของ US-001 ยังใช้ได้ อย่าลืมเลข Issue ใหม่ใน branch, commit footer และ `Closes` ของ MR หากงานเดิมยังรอ review ให้ทำ review นั้นให้จบก่อนเริ่ม Capstone บน main ที่ merge แล้ว”

## สไลด์ 21 · พร้อมลงมือแล้ว

“ตอนนี้เราเห็นเส้นทางทั้งคอร์สแล้ว ขั้นแรกคือเปิดบท 00 และตรวจเครื่องกับ repo ของตัวเองให้พร้อม หลังจากนั้นเราจะค่อย ๆ สร้างหลักฐานของ US-001 ทีละชิ้น ไม่ต้องจำทุกคำสั่งในวันนี้ แต่ให้จำหลักการว่า **คนกำหนดสิ่งที่ถูกต้อง, Agent เสนอและลงมือ, คนตรวจผลจากหลักฐานจริง**

ก่อนเริ่ม ให้ทุกคนเปิดหน้า Prerequisites แล้วดูว่าติดตั้งอะไรแล้วบ้าง ถ้ามีจุดที่ไม่ผ่าน ให้เก็บข้อความผิดพลาดและสถานะปัจจุบันไว้ เราจะใช้สิ่งนั้นช่วยกันแก้ตามขั้นตอน เมื่อพร้อมแล้วจึงไปสู่ Issue แรกของเรา”
